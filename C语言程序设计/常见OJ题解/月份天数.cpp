#include <stdio.h>

int main()
{
	int yy,mm,month[12]={31,28,31,30,31,30,31,31,30,31,30,31};
	//yy,mm是年，月；month的12个数对应12个年份
	scanf("%d%d",&yy,&mm);
	//获取用户输入，并且储存
	if (yy%4==0 && yy%100!=0 || yy%400==0)
	//先判断闰年
	//要用两个等于号才表示相等，一个等号不是这个意思
	month[1]=29;
	//一个等号表示左边的变量变成右边的值;
	printf("%d",month[mm-1]);
	//第一个数的序号是0,因此这里要减一，输出对应月份天数
	
	return 0;
	//程序终止语句
	//拿下
}