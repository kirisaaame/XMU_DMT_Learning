;默认采用ML6.11汇编程序
DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    MOV AX,1470H
    AND AX,AX
    OR AX,AX
    XOR AX,AX
    NOT AX
    TEST AX,0F0F0H
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START